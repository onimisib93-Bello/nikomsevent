Put Nikoms' own photos and videos here, then point the entries in
`src/content/media.ts` at them (e.g. `src: "/media/main-hall.jpg"`).
Set `heroVideo` in that file to `/media/hero.mp4` for a looping hero video.
Aim for 2400px-wide JPG/WebP images; Next.js resizes them automatically.
