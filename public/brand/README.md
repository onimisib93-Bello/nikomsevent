Put the Nikoms logo files here (SVG preferred), e.g. `logo.svg` and `logo-light.svg`.
Then replace the text wordmark in `src/components/layout/Header.tsx` and
`src/components/layout/Footer.tsx`, and update the brand colours in
`src/styles/tokens.css` to match the logo.
